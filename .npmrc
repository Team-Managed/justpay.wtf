# Automatically installs peer dependencies without prompting
auto-install-peers=true
strict-peer-dependencies=false

# Flatten node_modules for Azure App Service compatibility
# Prevents symlink resolution failures in the Node.js runtime
node-linker=hoisted
